name: Video Downloader & Cutter Ultra HD Max Bitrate

on:
  workflow_dispatch:
    inputs:
      link_youtube:
        description: 'Link Video Sumber'
        required: true
      waktu_mulai:
        description: 'Waktu Mulai (Contoh: 00:00:00)'
        required: true
        default: '00:00:00'
      waktu_selesai:
        description: 'Waktu Selesai (Contoh: 00:00:30)'
        required: true
        default: '00:00:30'

jobs:
  download_cutter:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Checkout Repository
        uses: actions/checkout@v4

      - name: 2. Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 3. Install Tools
        run: |
          sudo apt-get update && sudo apt-get install -y ffmpeg jq curl
          sudo curl -L https://github.com/yt-dlp/yt-dlp-nightly-builds/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp
          sudo chmod a+rx /usr/local/bin/yt-dlp
          yt-dlp -U

      - name: 4. Download Full Video (Kualitas Mentah Paling Tinggi Tanpa Batas)
        env:
          YT_COOKIES: ${{ secrets.YT_COOKIES }}
        run: |
          set +e

          if [ -n "$YT_COOKIES" ] && [ "$YT_COOKIES" != "KOSONG" ]; then
            echo "$YT_COOKIES" > cookies.txt
            COOKIE_ARG="--cookies cookies.txt"
          else
            COOKIE_ARG=""
          fi

          FIXED_UA="Mozilla/5.0 (Linux; Android 10; K) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Mobile Safari/537.36"

          echo "🚀 Mendownload video sumber kualitas tertinggi..."

          yt-dlp \
            $COOKIE_ARG \
            --js-runtime node \
            --force-ipv4 \
            --no-cache-dir \
            --user-agent "$FIXED_UA" \
            --extractor-args "youtube:player-client=android,web" \
            -N 5 \
            -f "bestvideo+bestaudio/best" \
            --merge-output-format mp4 \
            -o "video_mentah.mp4" \
            "${{ github.event.inputs.link_youtube }}"

          EXIT_CODE=$?
          rm -f cookies.txt

          if [ $EXIT_CODE -ne 0 ] || [ ! -f "video_mentah.mp4" ]; then
            echo "❌ Download gagal!"
            exit 1
          fi

          echo "✅ Download video mentah selesai. Ukuran file mentah:"
          ls -lh video_mentah.mp4

      - name: 5. Potong Video Ultra Tajam (Max Bitrate & Preset Very Slow)
        run: |
          echo "✂️ Memotong video secara presisi dengan kualitas pixel maksimal..."
          
          # -crf 10 dengan -preset veryslow memaksa encoder memproses detail terkecil pixel agar sangat tajam
          # -b:v 0 membebaskan batas bitrate agar mengambil berapapun yang dibutuhkan sumber aslinya
          ffmpeg -y -ss "${{ github.event.inputs.waktu_mulai }}" \
                 -to "${{ github.event.inputs.waktu_selesai }}" \
                 -i "video_mentah.mp4" \
                 -c:v libx264 -crf 10 -preset veryslow \
                 -b:v 0 -maxrate 50M -bufsize 100M \
                 -pix_fmt yuv420p \
                 -c:a aac -b:a 320k \
                 -avoid_negative_ts make_zero \
                 "hasil_potongan.mp4"

          echo "✅ Pemotongan selesai! Cek ukuran file hasil akhir:"
          ls -lh hasil_potongan.mp4
          
          # Cek detail resolusi & bitrate asli di log
          ffprobe -v error -select_streams v:0 -show_entries stream=width,height,codec_name,bit_rate -of default=noprint_wrappers=1 hasil_potongan.mp4

      - name: 6. Upload Hasil Video ke Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: video-hasil-potongan
          path: hasil_potongan.mp4
          retention-days: 1
          
