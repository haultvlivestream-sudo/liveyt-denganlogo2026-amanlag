name: Live Record to Telegram Bot (Ultimate Fix)

on:
  workflow_dispatch:
    inputs:
      link_youtube:
        description: 'Link Video Sumber'
        required: true
      durasi_detik:
        description: 'Durasi Rekam dalam Detik (Contoh: 633)'
        required: true
        default: '633'

jobs:
  record_to_telegram:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Checkout Repository
        uses: actions/checkout@v4

      - name: 2. Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 3. Install Tools
        run: |
          sudo apt-get update && sudo apt-get install -y ffmpeg jq curl
          sudo curl -L https://github.com/yt-dlp/yt-dlp-nightly-builds/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp
          sudo chmod a+rx /usr/local/bin/yt-dlp

      - name: 4. Proses Rekam Video (Tanpa Ribet)
        env:
          YT_COOKIES: ${{ secrets.YT_COOKIES }}
        run: |
          set +e

          if [ -n "$YT_COOKIES" ] && [ "$YT_COOKIES" != "KOSONG" ]; then
            echo "$YT_COOKIES" > cookies.txt
            COOKIE_ARG="--cookies cookies.txt"
          else
            COOKIE_ARG=""
          fi
          
          LOGO_FILE="logoihyajuni2026.mp4"
          INTRO_FILE="intrologoihya7tvjuli2026.mp4"

          if [ ! -f "$LOGO_FILE" ] || [ ! -f "$INTRO_FILE" ]; then
            echo "ERROR: File logo atau intro tidak ditemukan!"
            exit 1
          fi

          INTRO_DURATION=$(ffprobe -v error -show_entries format=duration -of default=noprint_wrappers=1:nokey=1 "$INTRO_FILE")
          FADE_START=$(awk "BEGIN {print $INTRO_DURATION - 3}")
          if (( $(echo "$FADE_START < 0" | bc -l) )); then
            FADE_START=0
          fi

          FIXED_UA="Mozilla/5.0 (Linux; Android 10; K) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Mobile Safari/537.36"

          echo "--- Mengambil URL Video ---"
          VIDEO_URL=$(yt-dlp $COOKIE_ARG \
                              --js-runtime node \
                              --force-ipv4 \
                              --no-cache-dir \
                              --user-agent "$FIXED_UA" \
                              --extractor-args "youtube:player-client=android,web" \
                              -f "bestvideo+bestaudio/best" \
                              -g "${{ github.event.inputs.link_youtube }}" 2>/dev/null)

          if [ -z "$VIDEO_URL" ]; then
            echo "❌ GAGAL MENDAPATKAN URL VIDEO!"
            exit 1
          fi

          DURATION="${{ github.event.inputs.durasi_detik }}"

          echo "🚀 Menjalankan FFmpeg (Durasi: $DURATION detik)..."

          # Eksekusi bersih tanpa -ss yang bikin timeout, langsung batasi pakai -t
          ffmpeg -y -i "$VIDEO_URL" \
            -stream_loop -1 -i "$LOGO_FILE" \
            -i "$INTRO_FILE" \
            -t "$DURATION" \
            -filter_complex "[1:v]colorkey=0x006900:0.1:0.1,scale=iw*0.15:-1[logo]; [0:v][logo]overlay=W-w-(W*0.02):(W*0.02)[main_with_logo]; [2:v]scale=1920:1080,format=pix_fmts=yuva420p,fade=t=out:st=$FADE_START:d=3:alpha=1[intro_faded]; [main_with_logo][intro_faded]overlay=x='(W-w)/2':y='(H-h)/2':format=auto:enable='lte(t,$INTRO_DURATION)'" \
            -c:v libx264 -preset ultrafast -crf 23 \
            -pix_fmt yuv420p \
            -c:a aac -b:a 128k \
            "hasil_rekaman.mp4"

          if [ ! -f "hasil_rekaman.mp4" ]; then
            echo "❌ GAGAL: File hasil rekaman tidak tercipta!"
            exit 1
          fi

          echo "✅ Berhasil merender!"

      - name: 5. Kirim Video ke Telegram Bot
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          echo "📤 Mengirim video ke Telegram..."
          curl -F chat_id="$TELEGRAM_CHAT_ID" \
               -F document=@"hasil_rekaman.mp4" \
               -F caption="✅ Berhasil! Video sukses terkirim ke Telegram." \
               "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendDocument"
          echo "✨ Selesai!"
          
